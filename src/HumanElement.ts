import { html, css, LitElement } from 'lit-element'

export class HumanElement extends LitElement {
  static styles = css`
    :host {
        width: 16px;
        height: 16px;
        padding: 25px;
        background-color: black;
      }
  `

  render () {
    return html``
  }
}
